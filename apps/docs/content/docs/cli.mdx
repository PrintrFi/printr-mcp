---
title: CLI
description: "@printr/cli — configure MCP servers and install agent skills."
---

`@printr/cli` wires Printr into your environment: it configures MCP servers and installs agent
skills.

## Run it

```bash
bunx @printr/cli setup
```

_TODO: document the `setup` and `skill` sub-commands and their options._
